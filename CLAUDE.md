@AGENTS.md
# ルール
- 必ず日本語で応答すること
- 確認メッセージも日本語で出すこと
- コードのコメントも日本語で書くこと

# 自動承認
- ls, cat, pwd, echo, grep, find などの読み取り専用コマンドは確認不要で実行してよい
- npm run, npm test, git status, git log, git diff も確認不要
- rm, mv, git push, sudo は必ず確認を取ること